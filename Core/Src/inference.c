/**
 * @file inference.c
 * @brief TFLite inference engine wrapper.
 * Expected to execute the quantized CNN (int8) for 4-class gesture classification.
 */
#include "inference.h"

void Inference_Init(void) {}
int Inference_Classify(float* features) { return 0; }
