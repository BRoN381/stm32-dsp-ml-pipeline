/**
 * @file dma_buffer.c
 * @brief Ring buffer + DMA configuration.
 * Expected to implement CPU-free data transfer at 100Hz using double-buffering.
 */
#include "dma_buffer.h"

void DMA_Init(void) {}
void DMA_StartTransfer(void) {}
